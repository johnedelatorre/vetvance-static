// add page to namespace
VETAPP.namespace('VETAPP.pages.advance');

// create page
VETAPP.pages.advance = (function() {

	var self = {};

	self.init = function init() { console.log('init page: advance.js');

		var self = this;
		
		self.isMobile = window.orientation || Modernizr.touch;
		self.container = $('.main-container');
		
		// init Modules
		self.initModules();
		// Bind events
		self.bindEvents();
	}

	self.initModules = function()
	{
		var self = this;
	}
	self.bindEvents = function()
	{	
		var self = this;
	}

	return self;

}());

$(document).ready(function(){
	VETAPP.pages.advance.init();	
});

/*
$(document).ready(function(){
	// $('#student-status').selectize();
	// $('#loan-status').selectize();
	// $('#job-status').selectize();
})
*/
;
